# FastAPI test

Testing FastAPI with a TODO app.

## Requirements
- Python (>= 3.6)
- [pipenv](https://pipenv.pypa.io)

## Quickstart

1. Install dependencies
    ```shell script
    pipenv sync
    ```
2. Run app
    ```shell script
    pipenv run start
    ```
